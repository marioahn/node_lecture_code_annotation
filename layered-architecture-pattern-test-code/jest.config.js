export default {
  // 해당 패턴에 일치하는 경로가 존재할 경우 테스트를 하지 않고 넘어간다
  testPathIgnorePatterns: ['/node_modules/'],
  // 테스트 실행 시 각 TestCase를 출력한다
    // verbose: 즉, 테수트 코드 내부에 있는 세부 사항까지 출력해줌ㅇㅇ
  verbose: true,
};